# Airflow-Dynamic-Loader
Airflow Dynamic Loader automates loading data from APIs into DuckDB, with dynamic endpoints, JSON saving, conditional insert or file load based on row count, automatic table creation, and data quality checks. Demonstrates full Airflow DAG orchestration and branching.
